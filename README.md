# Task Manager – запуск проекта

## Запуск в Docker
1. В корне проекта:
   ```bash
   npm run compose:up
   ```
2. Приложение будет доступно по адресу `http://localhost:4173`, Swagger `http://localhost:3000/api`.
3. В папке backend 
   ```bash
   npm run seed
   ```
